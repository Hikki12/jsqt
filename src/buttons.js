import { Base } from './Base';

class Button extends Base {
    constructor(id){
        super(id);
    }
};

class ToogleButton extends Base {
    constructor(id){
        super(id);
    }
}

export {
    Button
}